server {
    listen 80;
    listen [::]:80;
    server_name recruitment.cvbjas.com;

    root /var/www/recruitment.cvbjas.com;

    location ^~ /.well-known/acme-challenge/ {
        try_files $uri =404;
    }

    location / {
        default_type text/plain;
        return 200 "recruitment.cvbjas.com bootstrap aktif\n";
    }
}
